export function LoginPage() {
  return (
    <main className="login-screen">
      <section className="login-hero">
        <p className="section-eyebrow">Prototype</p>
        <h1 className="login-title">Login flow placeholder</h1>
        <p className="login-copy">The interactive authentication flow arrives in a later milestone.</p>
      </section>
    </main>
  );
}
